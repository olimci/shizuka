# shizuka

my static site generator.

install witth

```sh
go install github.com/olimci/shizuka
```

create a new static site in the current dir using the `init` command

```sh
shizuka init
```

start a dev server with `dev`

```sh
shizuka dev
```
