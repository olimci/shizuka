<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{{.Page.Title}} | {{.Site.Title}}</title>
        <meta name="description" content="{{.Page.Description}}" />
        <link rel="stylesheet" href="/style.css" />
    </head>
    <body>
        <div class="page">
            <header class="site-header">
                <div class="brand">
                    <a class="logo" href="{{.Site.URL}}">{{.Site.Title}}</a>
                    <p class="tagline">{{.Site.Description}}</p>
                </div>
                <nav class="nav">
                    <a href="{{.Site.URL}}">Home</a>
                    <a href="{{.Site.URL}}#posts">Posts</a>
                </nav>
            </header>

            <main>
                <article class="article">
                    <div class="article-header">
                        <p class="eyebrow">Post</p>
                        <h1 class="article-title">{{.Page.Title}}</h1>
                        {{if .Page.Description}}
                        <p class="lead">{{.Page.Description}}</p>
                        {{end}}
                        <div class="article-meta">
                            {{if not (.Page.Date.IsZero)}}<span
                                >{{.Page.Date.Format "January 2, 2006"}}</span
                            >{{end}} {{if not (.Page.Updated.IsZero)}}<span
                                >Updated {{.Page.Updated.Format "January 2, 2006"}}</span
                            >{{end}}
                        </div>
                        {{ if .Page.Tags }}
                        <div class="tags">
                            {{ range .Page.Tags }}<span class="tag"
                                >{{ . }}</span
                            >{{ end }}
                        </div>
                        {{ end }}
                    </div>
                    <div class="content">{{.Page.Body}}</div>
                </article>
            </main>

            <footer class="site-footer">
                <p>
                    Back to <a href="{{.Site.URL}}">home</a> or browse the
                    <a href="{{.Site.URL}}#posts">post list</a>.
                </p>
            </footer>
        </div>
    </body>
</html>
