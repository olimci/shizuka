# TODO

## Potential

- create a minifier wrapper type, so we can provide a better interface?
- parseTemplateGlob might do well to be made more customisable so we can attatch funcs in a more sensible way?
- version type + version comparison functions?
- PageData feels like a redundant abstraction, considering removing it (what do we replace it with?)
